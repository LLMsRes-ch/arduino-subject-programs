// You are an experienced software developer. Write an optimized Arduino Uno Rev3 code that checks if a selected size which is MEDIUM matches a one of a predefined values (SMALL, MEDIUM, LARGE). If it matches MEDIUM, set a variable to true; otherwise, remain false. Finally, print the result (Match or No match). Set the selected size to SMALL.
// Strategy 1

void setup() {
Serial.begin(9600);
byte SMALL= 1, MEDIUM = 2, LARGE = 3;
byte selectedSize = 1;
bool result=false;
if (selectedSize == MEDIUM)
result=true;
Serial.println(result ? "Match" : "No match");
}
void loop() {}
