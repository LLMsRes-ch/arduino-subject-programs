// You are an experienced software developer. Write an optimized Arduino Uno Rev3 code that generates a message indicating that "pin number 1: is working well". a string should grow in size, first empty string, append "pin number", append "1:", append "is working well", then print the string.
// Strategy 2

void setup() {
Serial.begin(9600);
String str;
str = "Pin Number";
str = str + "= 1: ";
str = str + "is working well";
Serial.println(str);
}
void loop() {}
